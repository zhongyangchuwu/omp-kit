# Copy to <agent-root>/.env, or set these in the environment that launches OMP.
# Never commit real keys. CPA is required for the default model roles; DeepSeek is optional.
CPA_API_KEY=
DEEPSEEK_API_KEY=
