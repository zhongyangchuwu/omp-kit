name: paper-diagram
kind: skill
status: draft
path: drafts/paper-diagram

source:
  type: self
  origin: null
  references:
    - label: ppt-master
      path: references/ppt-master
      repository: null
      branch: null
      commit: null
    - label: anthropic-pptx
      path: references/anthropic-skills/skills/pptx
      repository: https://github.com/anthropics/skills.git
      branch: main
      commit: null
  imported: null
  promoted: null
  notes:
    - "SVG backend rules derived from ppt-master SVG compatibility standards."
    - "soffice.py sandbox logic localized from anthropic-pptx into scripts/pptx_export.py."
    - "Default workflow is SVG-first; PPTX backend remains available for editable PowerPoint output."

risk:
  level: medium
  reason: >
    Bundled scripts run external converters (rsvg-convert, soffice, pdftoppm)
    as subprocesses for SVG/PPTX→PDF→JPG export. PPTX export may compile C
    code as a sandbox fallback. No network access, no API keys, no secrets.

activation:
  mode: not-applicable
  notes: Draft only; removed from active skills after local use showed the workflow is not ready.

verification:
  commands:
    - "python3 -c \"import pptx; print('python-pptx OK')\""
    - "python3 -c \"from PIL import Image; print('Pillow OK')\""
    - "which rsvg-convert || echo 'WARNING: rsvg-convert not found'"
    - "which soffice || echo 'WARNING: soffice not found'"
    - "which pdftoppm || echo 'WARNING: pdftoppm not found'"
  notes:
    - "Default SVG backend uses scripts/svg_export.py."
    - "PPTX fallback uses scripts/pptx_layout.py and scripts/pptx_export.py."

maintenance:
  last_reviewed: "2026-06-08"
  notes:
    - "Initial creation as PPTX-first skill."
    - "Refactored to SVG-first with unified Figure Spec and PPTX fallback."
    - "Demoted from active skills to drafts because current workflow is not useful enough in practice."
    - "Color palettes and spacing derived from academic publishing conventions."
    - "Sandbox shim code localized from anthropic-skills/pptx/scripts/office/soffice.py."

relationships:
  upstream:
    - references/ppt-master
    - references/anthropic-skills/skills/pptx
