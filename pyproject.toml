[project]
name = "pi-kit"
version = "0.1.0"
description = "Pi / Oh My Pi capability workbench"
requires-python = ">=3.12"
dependencies = [
    "jsonschema>=4.25.1",
    "Pillow>=9.0.0",
    "pyyaml>=6.0.3",
    "python-pptx>=0.6.21",
    "pytest>=9.0.3",
]
